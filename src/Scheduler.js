function Scheduler(){
}

// creates a mapping between an event
// and a sound played
Scheduler.prototype.on = function(fileInfo){
};

// triggers event on all listeners
Scheduler.prototype.trigger = function(eventName){
};

module.exports = Scheduler;
